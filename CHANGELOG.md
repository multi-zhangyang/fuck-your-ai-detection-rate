# Changelog

## Unreleased

### Added

- DOCX 和 TXT 两种文档流程。
- 标准、细致和长段三档段内分块，默认使用标准分块。
- 单模板 1–3 轮改写与 1–16 并发处理。
- Chat Completions 与 Responses 实时返回支持。
- 模型连接向导、DeepSeek 官方连接和多个 OpenAI 兼容连接。
- 逐段对照、保留原文、手动编辑、重新改写和断点继续。
- 完成后基于当前审阅结果继续改写，并允许重新选择模型、提示词方案和处理设置。
- 可搜索、筛选和调整正文范围的保护区地图。
- 最近文档打开、继续、导出和删除。

### Changed

- DOCX 导出改为在原文件包中定点替换所选正文的既有文字节点。
- 模型连接与提示词方案分开管理，一次任务固定一个模型和一份执行步骤快照。
- 数字、引用、URL 和用户保护词的变化统一作为提醒处理。
- 完整非空的模型结果优先保存和展示，提醒不会触发自动回退、自动重试或导出阻断。
- 运行中只显示整体段落进度，完成后再进入逐段审阅。
- 界面统一使用 shadcn/ui 组件，并适配桌面、平板和手机宽度。
- 最近文档使用本地 JSON 数据，最多保留 20 条。

### Removed

- 学校格式规则解析和自动重排。
- 质量评分、收益评分、AI 风险评分和候选结果门禁。
- 自动批量修复、每轮模型路由和复杂诊断中心。
- SQLite 历史治理、数据库修复、孤儿扫描和备份压缩。

### Fixed

- 目录中的“参考文献”“致谢”“附录”条目不再错误截断后续正文范围。
- 正文中的行内公式、图片和域保持固定，其前后的普通文字仍可改写。
- 英文正文被改成中文时会给出提醒，但不会覆盖模型结果。
- 实时返回完成事件会立即结束读取；正文开始后的连接中断只暂停当前分块。
- 刷新或重启后能够恢复当前文档和任务，继续时只运行未完成步骤。
- 完成任务的下一轮设置会真实写入新任务快照，并用于模型请求；中断任务仍按原快照断点继续。
- API Key 留空时保留已保存密钥，也可以明确替换或清除。
- 未完成段落可以由用户手动补写或保留原文后继续导出。
- 模型、提示词、正文范围和逐段审阅页面不再出现横向溢出或无限向下延伸。
